'use strict';

var cx = React.addons.classSet;

var MealHistory = React.createClass({
  displayName: 'MealHistory',
  render: function() {
    return (
      <div>
      </div>
    )
  }
});

module.exports = MealHistory;
